#include <bits/stdc++.h>
using namespace std;
int t, a1, a2, b1, b2;
int main() {
ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    cin >> t;
    while (t--) {
        cin >> a1 >> a2 >> b1 >> b2;

    }
}